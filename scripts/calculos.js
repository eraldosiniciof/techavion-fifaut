function calcularTotalCompra(lista) {
    let totalC = 0
    lista.forEach(item => {
        totalC += item
    })
    return totalC
}

function calcularTotalVendas(lista) {
    let totalV = 0
    lista.forEach(item => {
        totalV += item
    })
    return totalV
}